{% extends 'base.html.twig' %}

{% block title %}New Question{% endblock %}

{% block body %}
    <div class="row">
        <div class="col">
            <h1>Create new Question</h1>

            {{ include('question/_form.html.twig') }}

            <a class="btn btn-secondary" href="{{ path('question_index') }}">Back</a>
        </div>
    </div>
{% endblock %}
